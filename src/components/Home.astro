---

---
<section class="p-2 md:p-8 " id="home">
    <div class="flex flex-wrap justify-between m-2 md:my-10 mx-6 gap-y-8">
        <div class="detail flex flex-col gap-y-4 md:gap-y-6 md:py-12"><!---Sarah -->
            <h1 class="text-2xl md:text-3xl lg:text-5xl font-semibold text-white">Me llamo <span style="color:#00E8F8;">Eduardo Tolaba</span></h1>
            <p class="text-cyan-400 text-xl md:text-4xl">Soy <span class="text-white font-semibold" id="subtitle"></span></p>
            <div class="develop-title text-sm md:text-2xl font-semibold relative uppercase">
                <span class="text__develop">y Estudiante en Desarrollo de Software</span>
                <span class="text__develop-hover">y Estudiante en Desarrollo de Software</span>
            </div>
            <div class="flex flex-wrap pt-4 pb-4 gap-5 social">
                <a href="https://github.com/TolabaE" target="_blank" class="text-base w-8 h-8 md:w-12 md:h-12 md:text-2xl flex justify-center items-center rounded-full text-cyan-400 border-2 border-cyan-400"><i class="bi bi-github"></i></a>
                <a href="https://www.linkedin.com/in/tolaba-eduardo-esequiel/" target="_blank" class="text-base w-8 h-8 md:w-12 md:h-12 md:text-2xl flex justify-center items-center rounded-full text-cyan-400 border-2 border-cyan-400"><i class="bi bi-linkedin"></i></a>
                <a href="#contact" class="text-base w-8 h-8 md:w-12 md:h-12 md:text-2xl flex justify-center items-center rounded-full text-cyan-400 border-2 border-cyan-400"><i class="bi bi-envelope-fill"></i></a>
            </div>
        </div>
    </div>
</section>
<script>
    document.addEventListener("DOMContentLoaded", () => {
    const etiqueta = document.getElementById('subtitle');
    const texts = "Desarrollador Web Full Stack       ";
    let long = texts.length;
    let count = 0; 

    function writeTextDom() {
        if (etiqueta) {  // Verificación de null
            if (count < texts.length) {
                etiqueta.textContent += texts.charAt(count); // Corta la letra de acuerdo a la posición del contador.
                count++;
            } else {
                deleteTextDom(); // Si el contador llega al límite de la longitud, se ejecuta esta función
            }
        }
    }

    function deleteTextDom() {
        if (etiqueta) {  // Verificación de null
            if (count === texts.length) {
                etiqueta.textContent = texts.substring(0, long);
                long--;
                if (long === -2) {
                    etiqueta.textContent = '';
                    long = texts.length;
                    count = 0;
                }
            }
        }
    }
    setInterval(writeTextDom, 100);//se usa para determinar el tiempo de ejecucion de la funcion y su duracion
});

</script>
<style>
    #home{
        background-image: url('/home-image-portal.jpg');
        background-repeat: no-repeat;
        background-size:cover;
        background-position:center;
        height: 100vh;
    }
    
    .social a:hover {
        color: #1F252E;
        background-color: #00E8F8;
        box-shadow: 0 0 20px #00E8F8;
        transition: all .3s ease-in-out;
    }
    .images {
        /* background-image: url("https://images.unsplash.com/photo-1478760329108-5c3ed9d495a0?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxleHBsb3JlLWZlZWR8MXx8fGVufDB8fHx8fA%3D%3D&w=1000&q=80");
        background-repeat: no-repeat;
        background-position: 0 -75px; */
        /* -webkit-box-shadow: 0 10px 10px -10px #000000; */
        /* -moz-box-shadow: 0 10px 10px -10px #000000; */
        box-shadow: 0px 16px 4px -10px rgb(0, 232, 248);
        width: 528px;
        background-size: cover;
    }
    .text__develop{
        color: transparent;
        -webkit-text-stroke:1px rgba(255,255,255, 0.6);
    }
    .text__develop-hover{
        position: absolute;
        inset: 0;
        width: 0%;
        color: #00E8F8;
        overflow: hidden;
        transition: 0.5s ease-in-out;
        border-right: 3px solid #00E8F8;
    }
    .develop-title:hover .text__develop-hover{
        width: 95%;
        filter: drop-shadow(0 0 30px #1af7ff);
    }
    @keyframes check {
        0% {
            transform: translateY(-100px);
            opacity: 0;
        }

        100% {
            transform: translateY(0);
            opacity: 1;
        }
    }
    @keyframes textanime {
        0% {
            width: 0%;
        }

        50% {
            width: 100%;
        }

        100% {
            width: 0%;
        }
    }
</style>
